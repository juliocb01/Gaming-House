<div class="container mt-5">
  <h1>bookings</h1>

  <% @bookings.each do |booking| %>
    <%= link_to booking_path(booking) do %>
      <p class="booking-card"><%= booking.bootcamp.name %></p>
    <% end %>
  <% end %>
</div>
