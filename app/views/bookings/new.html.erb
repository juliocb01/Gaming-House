<div class="container">
  <h1>Create a booking</h1>

<%= simple_form_for([@bootcamp, @booking]) do |f| %>
<%= f.input :start_date %>
<%= f.input :end_date %>
<%= f.button :submit %>
<% end %>
<%= link_to 'Back', bootcamps_path(@booking) %>

</div>
